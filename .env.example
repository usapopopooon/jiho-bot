DISCORD_TOKEN=
# 1プロセスで複数Botを並走させたい場合は CSV で指定 (両方指定もOK、重複は除外)
# DISCORD_TOKENS=token1,token2,token3
# 開発時のみ。指定するとコマンドが即時同期される(空ならグローバル同期)
DISCORD_GUILD_IDS=
# 時報のタイムゾーン (デフォルト Asia/Tokyo)
JIHO_TIMEZONE=Asia/Tokyo
# 毎時30分の時報は env では設定しない。 /jiho half:true で guild ごとに有効化
LOG_LEVEL=INFO
